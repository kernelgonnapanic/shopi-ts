import styled from 'styled-components';

export const FooterContainer = styled.footer`
  display: flex;
  align-items: center;
  position: absolute;
  bottom: 0;
  height: 50px;

  p {
    margin: 0;
  }
`;
